# `universal_machinery.validation`

Structural validator.  Returns a `list[ValidationError]` (one entry per issue); never raises.

::: universal_machinery.validation
    options:
      members:
        - validate
        - ValidationError
